#### 1. Где хранятся пользовательские и системные настройки подключения?
Пользовательские настройки SSH хранятся в файле `~/.ssh/config` на локальном компьютере, а системные - в `/etc/ssh/ssh_config`.

---
#### 2. Что за файл options?
Это конфигурационный файл SSH-клиента, где можно задавать индивидуальные параметры подключения для разных хостов, чтобы не вводить их каждый раз вручную.

---
#### 3. Отредактируйте файл options так, чтобы можно было подключаться не вводя имя пользвателя и порт
Открываем `nano ~/.ssh/config` и вписываем данные моего сервера:

![Настройка SSH config](images/Pasted%20image%2020251224013106.png)

---
#### 4. Назовите подключение удобным для вас спсобом
Как видно на скрине мой сервер будет называться - `myserver`.

---
#### 5. Проверьте работоспособность
Теперь, чтоб зайти на сервер, достаточно простой команды `ssh myserver` :

![Подключение через алиас](images/Pasted%20image%2020251224013335.png)