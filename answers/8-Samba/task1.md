
#### 1. Установите пакет samba
Устанавливаем `samba`:

![Установка samba](images/Pasted%20image%2020251114151250.png)

---

#### 2. Что такое общая папка, зачем оно может быть нужно?
Общая папка - это папка, доступ к которой можно предоставить нескольким пользователям или устройствам для совместной работы с файлами. Компьютеры, объединенные в одну сеть, могут получать доступ к общей папке. Пользователи могут просматривать, изменять или добавлять файлы, в зависимости от предоставленных прав доступа.

---
#### 3. Создайте общую папку без пароля с правами только на чтение файлов
Создаем рекурсивно папку `public_readonly` и устанавливаем на нее права только на чтение, кроме владельца, владелец будет иметь все права, (`755`). Для того чтобы изменить права только на чтение у ВСЕХ, в команде `chmod ` надо указать (`555`): 

![Настройка папки только чтение](images/Pasted%20image%2020251222184207.png)

Для настройки работы самой `Samba`, надо открыть `smb.conf` и прописать имя, путь, видимость папки в списке сетевых ресурсов и т.д.

![Конфиг Samba только чтение](images/Pasted%20image%2020251222184812.png)

---
#### 4. Создайте общую папку с паролем с правами на чтение и запись
Создаем  папку `secure_rw` и устанавливаем на нее права на чтение и запись только у владельца и группы (`770`).

![Создание защищенной папки](images/Pasted%20image%2020251222185539.png)

Классически создаем пользователя `smbuser`, после этого создаем пользователя в Samba и задаем пароль, который и будет использоваться для доступа к папке, команда:
`smbpasswd -a smbuser`
Прописываем в `smb.conf`:

![Конфиг Samba с паролем](images/Pasted%20image%2020251222185434.png)

---
#### 5. Создайте общую папку с доступом для какой-то группы с полными правами
Для начала создадим группы пользователей для которых папка будет с полными правами, создаем два пользователя и проверяем что группа существует и все прекрасно, далее добавляем этих же пользователей в `Samba`:

![Создание группы пользователей](images/Pasted%20image%2020251222190306.png)

Создаем папку, настраиваем права доступа для группы (`2` в начале (`setgid`) — новые файлы будут автоматически принадлежать группе `fullaccess`):

![Настройка прав для группы](images/Pasted%20image%2020251222190543.png)

Далее прописываем в `smb.conf` (доступ разрешён **всем пользователям группы fullaccess**):

![Конфиг для группового доступа](images/Pasted%20image%2020251222191312.png)

p.s. Доказательство того что я все это делаю совсем не зря, попробуем к этой папке подключиться:

![Подключение к папке](images/Pasted%20image%2020251222191421.png)

---
#### 6. Создайте общую папку в которой у одной группы будет полный доступ, а у другой только доступ на чтение. Третья группа не должна иметь к ней доступа.
Cоздаем папку `mixed_access`. Создаем 3 папки, для каждой папки по пользователю, каждого пользователя регистрируем в `Samba`:

![Создание пользователей для разных прав](images/Pasted%20image%2020251222191916.png)

Задаем разные права для разных групп:

![Разные права для групп](images/Pasted%20image%2020251222191937.png)
Далее прописываем в `smb.conf` (доступ разрешён пользователям групп `admins, readers`, только `admins` могут писать, по умолчанию только чтение для остальных):

![Конфиг смешанного доступа](images/Pasted%20image%2020251222192219.png)
