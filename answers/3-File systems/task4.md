# Продолжаем

#### 1. Raid массивы, что такое и какие бывают?
**RAID** (Redundant Array of Independent Disks) — технология объединения нескольких физических дисков в один логический для повышения производительности, отказоустойчивости или объёма хранилища.
##### Виды RAID:
- **RAID 0 (Striping — чередование)**. Данные разделяются на блоки и равномерно распределяются («располосовываются») между всеми дисками в массиве. Минимум дисков - 2.
- **RAID 1 (Mirroring — зеркалирование)**. Данные записываются одновременно на оба диска — создаётся точная копия («зеркало»). Минимум дисков - 2.
- **RAID 5 (Striping с контролем чётности)** Данные и информация для восстановления (паритет) распределяются между всеми дисками. Паритет позволяет восстановить данные при выходе одного диска. Минимум дисков - 3.
- **RAID 6 (Striping с двойным паритетом)**. Аналог RAID 5, но с двумя блоками паритета. Позволяет выдержать одновременный отказ двух дисков. Минимум дисков - 4.
- **RAID 10 (RAID 1+0 — зеркалирование + чередование)**. Сочетает RAID 1 и RAID 0: сначала создаются зеркала (RAID 1), затем они объединяются в полосу (RAID 0). Минимум дисков - 4.

---
#### 2. Добавьте в виртуальную машину 2 диска отформатируйте их в ext4.
Из прошлого задания остался диск `linuxalt_2` уже с нужными настройками , создаем новый диск `linuxalt_3`.
![Добавление диска](images/Pasted%20image%2020251110104603.png)

Отформатируем второй диск в `ext4` (ext4 — тип файловой системы, /dev/sdb1— раздел на диске, где будет создана файловая система):

![Форматирование диска](images/Pasted%20image%2020251110105104.png)

(p.s. первый диск уже отформатирован в task3)

---
#### 3. Создайте из них raid 0 массив.
Создаю раздел диска `sdc`:
![Создание раздела](images/Pasted%20image%2020251110120640.png)

Cоздаю RAID 0 массив (`--level=0` — RAID 0 (чередование/striping).  
`/dev/md0` — имя программного RAID-устройства):

![Создание RAID 0](images/Pasted%20image%2020251110120934.png)

---
#### 4. Проверьте всё ли работает
Проверяю создание:

![Проверка RAID](images/Pasted%20image%2020251110121219.png)

---
#### 5. Удалите raid0 и создайте raid1
Останавливаю RAID 0 и очищаю суперблоки на обоих разделах:

![Остановка RAID 0](images/Pasted%20image%2020251110121641.png)
![Очистка суперблоков](images/Pasted%20image%2020251110122050.png)

Создаю RAID 1(`--level=1` — RAID 1 (чередование/striping).  
`/dev/md1` — имя программного RAID-устройства):

![Создание RAID 1](images/Pasted%20image%2020251110122332.png)

---
#### 6. В чём между ними разница?
В RAID 0 данные разделяются на блоки и равномерно распределяются  между всеми дисками в массиве, в RAID 1 данные записываются одновременно на оба диска - т.е. создаётся точная копия.

---
#### 7. Есть ли файловые системы которые поддерживают raid массивы без стороннего ПО
Да, примерами таких ФС являются **ZFS** и **Btrfs.** Они поддерживают программный RAID (без `mdadm` или другого стороннего ПО), потому что функции зеркалирования (RAID 1), чередования (RAID 0) и даже RAID-Z (аналог RAID 5/6 в ZFS) реализованы на уровне самой файловой системы.

---
#### 8. Можно ли создать raid массив во время установки системы?
Большинство дистрибутивов (Ubuntu, Debian, RHEL, CentOS) и в том числе ALT Linux позволяют создать  RAID-массив  прямо в установщике: при ручной разметке дисков есть пункт «Configure software RAID», где выбирают уровень (0, 1 и др.) и диски.

---